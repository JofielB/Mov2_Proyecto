<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!-- Activate animation from the schedule to the Visit Activity. -->
        <item name="android:windowContentTransitions">true</item>
    </style>

    <!-- Base application theme. -->
    <style name="Agendar" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">#E75838</item>
        <!-- Activate animation from the schedule to the Visit Activity. -->
        <item name="android:windowContentTransitions">true</item>
    </style>

    <style name="AppThemeTecLogin" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimaryTecL</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDarkTecL</item>
        <item name="colorAccent">@color/colorAccentTecL</item>
    </style>

</resources>
